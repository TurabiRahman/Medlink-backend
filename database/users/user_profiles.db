| Column                  | Type          | Notes                  |
| ----------------------- | ------------- | ---------------------- |
| id                      | UUID          | Primary key            |
| user_id                 | UUID          | References `users(id)` |
| first_name              | VARCHAR(100)  | Required               |
| last_name               | VARCHAR(100)  | Required               |
| gender                  | `gender_type` | Uses our ENUM          |
| date_of_birth           | DATE          | Required               |
| national_id             | VARCHAR(30)   | Unique                 |
| address                 | TEXT          | Required               |
| emergency_contact_name  | VARCHAR(200)  | Required               |
| emergency_contact_phone | VARCHAR(20)   | Required               |
| created_at              | TIMESTAMPTZ   | Auto timestamp         |
| updated_at              | TIMESTAMPTZ   | Auto timestamp         |


CREATE TABLE user_profiles (

    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),

    user_id UUID NOT NULL UNIQUE,

    first_name VARCHAR(100) NOT NULL,

    last_name VARCHAR(100) NOT NULL,

    gender gender_type NOT NULL,

    date_of_birth DATE NOT NULL,

    national_id VARCHAR(30) UNIQUE,

    address TEXT NOT NULL,

    emergency_contact_name VARCHAR(200) NOT NULL,

    emergency_contact_phone VARCHAR(20) NOT NULL,

    created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,

    updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT fk_user_profiles_user
        FOREIGN KEY (user_id)
        REFERENCES users(id)
        ON DELETE CASCADE

);


# Trigger for this table 

CREATE TRIGGER trg_user_profiles_updated_at
BEFORE UPDATE ON user_profiles
FOR EACH ROW
EXECUTE FUNCTION update_updated_at_column();


# After execution

medlink=# \d user_profiles;
                                 Table "public.user_profiles"
         Column          |           Type           | Collation | Nullable |      Default
-------------------------+--------------------------+-----------+----------+-------------------
 id                      | uuid                     |           | not null | gen_random_uuid()
 user_id                 | uuid                     |           | not null |
 first_name              | character varying(100)   |           | not null |
 last_name               | character varying(100)   |           | not null |
 gender                  | gender_type              |           | not null |
 date_of_birth           | date                     |           | not null |
 national_id             | character varying(30)    |           |          |
 address                 | text                     |           | not null |
 emergency_contact_name  | character varying(200)   |           | not null |
 emergency_contact_phone | character varying(20)    |           | not null |
 created_at              | timestamp with time zone |           | not null | CURRENT_TIMESTAMP
 updated_at              | timestamp with time zone |           | not null | CURRENT_TIMESTAMP
Indexes:
    "user_profiles_pkey" PRIMARY KEY, btree (id)
    "user_profiles_national_id_key" UNIQUE CONSTRAINT, btree (national_id)
    "user_profiles_user_id_key" UNIQUE CONSTRAINT, btree (user_id)
Foreign-key constraints:
    "fk_user_profiles_user" FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
Triggers:
    trg_user_profiles_updated_at BEFORE UPDATE ON user_profiles FOR EACH ROW EXECUTE FUNCTION update_updated_at_column()

# Update 2.0 

ALTER TABLE user_profiles
ALTER COLUMN first_name DROP NOT NULL,
ALTER COLUMN last_name DROP NOT NULL,
ALTER COLUMN gender DROP NOT NULL,
ALTER COLUMN date_of_birth DROP NOT NULL,
ALTER COLUMN address DROP NOT NULL,
ALTER COLUMN emergency_contact_name DROP NOT NULL,
ALTER COLUMN emergency_contact_phone DROP NOT NULL;